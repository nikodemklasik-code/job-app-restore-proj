export default function AutoApplyPage() {
  return (
    <section className="rounded-2xl border border-white/10 bg-white/[0.04] p-6 text-slate-200">
      <h2 className="text-lg font-semibold text-white">Auto Apply</h2>
      <p className="mt-2 text-sm text-slate-400">
        Auto Apply settings are not wired on this build branch. This fallback keeps Settings buildable until the full autopilot module is restored.
      </p>
    </section>
  );
}
