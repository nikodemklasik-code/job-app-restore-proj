import ComingSoonScreen from '@/components/coming-soon/ComingSoonScreen';

export default function CommunityComingSoonPage() {
  return (
    <ComingSoonScreen
      title="Community Centre"
      description="This screen is not wired to a live community feed yet. We are keeping the module visible so the navigation matches the full product map, but the real referrals, posts, events and sharing actions are still being connected."
      hint="Until the live community flow is ready, use Settings, Billing, Job Radar and Reports for the production-ready paths."
    />
  );
}
