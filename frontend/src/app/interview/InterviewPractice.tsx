export default function InterviewPractice() {
  return (
    <section className="rounded-2xl border border-white/10 bg-white/[0.04] p-6 text-slate-200">
      <h2 className="text-lg font-semibold text-white">Interview practice</h2>
      <p className="mt-2 text-sm text-slate-400">
        Interview practice is not wired on this build branch. This fallback keeps the route buildable until the full practice engine is restored.
      </p>
    </section>
  );
}
