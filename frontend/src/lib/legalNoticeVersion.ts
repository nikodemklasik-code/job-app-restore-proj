/**
 * Bump this when Terms / Privacy materially change AI or liability sections so the
 * in-app AI notice is shown again to signed-in users (see AiAssistedBanner).
 */
export const AI_ASSISTED_NOTICE_VERSION = 2;
