## Screen / slice

- **Screen ID(s):**  
- **Spec:** `screens/….md` (link)

## Explicit code (must match diff)

### Backend (`backend/`)

- [ ] List every new/changed file path (one per line) below, or `N/A` + reason:
```
```

### Frontend (`frontend/`)

- [ ] List every new/changed file path (one per line) below, or `N/A` + reason:
```
```

## Routes & API

- [ ] New/changed **URL routes** + file that registers them  
- [ ] **tRPC procedures** added/changed + **consumer file paths** on FE  

## Proof

- [ ] **Backend tests:** command + exit + commit  
- [ ] **Frontend tests:** command + exit + commit (or signed justification)  
- [ ] **`cd …/backend && npm run build`** → exit code:  
- [ ] **`cd …/frontend && npm run build`** → exit code:  

## QC

- Report path (if required):  
